# Privacy Policy

CheckrSpot does collect any data at all, so there is no concern about your personal data.

CheckrSpot does make three API calls to fetch data for each specimen:

1. Reverse geocoding: convert latitude + longitude into a location string like "US-CA, Riverside Co"

2. Fetch the elevation given latitude + longitude

3. Fetch the temperature given latitude + longitude + dateTime

